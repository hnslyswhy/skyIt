# Huanyu Wang's Assessment

## Checkout the repo locally

1. Download this repo to your local folder
2. Inside the folder, install the required dependencies and start the app by running:

```bash
  npm i
  npm start
```
